const initialState = {
  recipes: [],
  weeks: [],
  activeWeek: null,
};

const dietReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};

export { dietReducer };
